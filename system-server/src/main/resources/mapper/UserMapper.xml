<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.system.mapper.UserMapper">

    <resultMap id="UserMap" type="com.system.entity.User">
        <id column="id" property="id" typeHandler="com.system.handler.UUIDTypeHandler"/>
        <result column="username" property="username"/>
        <result column="password_hash" property="passwordHash"/>
        <result column="name" property="name"/>
        <result column="gender" property="gender"/>
        <result column="birth_date" property="birthDate"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
        <result column="campus_id" property="campusId" typeHandler="com.system.handler.UUIDTypeHandler"/>
        <result column="role" property="role"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <resultMap id="CampusUserVOMap" type="com.system.vo.CampusUserVO">
        <result column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="name" property="name"/>
        <result column="phone" property="phone"/>
        <result column="email" property="email"/>
        <result column="role" property="role"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="selectByUsername" resultMap="UserMap">
        SELECT * FROM user
        WHERE username = #{username} AND deleted = 0
    </select>

    <select id="selectById" resultMap="UserMap">
        SELECT * FROM user
        WHERE id = #{id} AND deleted = 0
    </select>

    <insert id="insertUser">
        INSERT INTO user (id, username, password_hash, name, gender, birth_date, phone, email, campus_id, role, create_time, update_time, deleted)
        VALUES (#{id}, #{username}, #{passwordHash}, #{name}, #{gender}, #{birthDate}, #{phone}, #{email}, #{campusId}, #{role}, #{createTime}, #{updateTime}, #{deleted})
    </insert>

    <select id="existsByUsername" resultType="boolean">
        SELECT COUNT(1) > 0
        FROM user
        WHERE username = #{username} AND deleted = 0
    </select>

    <select id="existsByPhone" resultType="boolean">
        SELECT COUNT(1) > 0
        FROM user
        WHERE phone = #{phone} AND deleted = 0
    </select>

    <select id="selectUsersByCampusId" resultMap="UserMap">
        SELECT * FROM user
        WHERE campus_id = #{campusId} AND deleted = 0
        ORDER BY create_time DESC
    </select>

    <update id="updateUser">
        UPDATE user
        SET username = #{username},
            name = #{name},
            gender = #{gender},
            birth_date = #{birthDate},
            phone = #{phone},
            email = #{email},
            campus_id = #{campusId},
            role = #{role},
            update_time = #{updateTime}
        WHERE id = #{id} AND deleted = 0
    </update>

    <select id="selectSpecialUsersByCampusIdAndRole" resultMap="CampusUserVOMap">
        SELECT u.id, u.username, u.name, u.phone, u.email, u.role, u.create_time
        FROM user u
        WHERE u.campus_id = #{campusId}
        AND u.deleted = 0
        <if test="role != null and role != ''">
            AND u.role = #{role}
        </if>
        ORDER BY u.create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

</mapper>