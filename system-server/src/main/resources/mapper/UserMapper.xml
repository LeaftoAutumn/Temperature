<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.system.mapper.UserMapper">

    <insert id="createUser" useGeneratedKeys="true" keyProperty="id">
        insert into user (id, username, password_hash, name, gender, birth_date, phone, email, campus_id, role, create_time, update_time, delete_flag)
        values
        (#{userId}, #{username}, #{password}, #{name}, #{gender}, #{birthDate}, #{phone}, #{email}, #{campusId}, #{role}, #{createTime}, #{updateTime}, #{deleted})
    </insert>

</mapper>