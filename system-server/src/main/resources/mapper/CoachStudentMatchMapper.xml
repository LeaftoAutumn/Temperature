<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.system.mapper.CoachStudentMatchMapper">

    <resultMap id="CoachStudentMatchMap" type="com.system.entity.CoachStudentMatch">
        <id column="id" property="id"/>
        <result column="student_id" property="studentId"/>
        <result column="coach_id" property="coachId"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <resultMap id="MatchRequestDetailVOMap" type="com.system.vo.MatchRequestDetailVO">
        <id column="id" property="id"/>
        <result column="student_id" property="studentId"/>
        <result column="student_name" property="studentName"/>
        <result column="coach_id" property="coachId"/>
        <result column="coach_name" property="coachName"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createdTime"/>
        <result column="update_time" property="updatedTime"/>
    </resultMap>

    <select id="selectByStudentAndCoach" resultMap="CoachStudentMatchMap">
        SELECT * FROM coach_student_match
        WHERE student_id = #{studentId}
          AND coach_id = #{coachId}
          AND deleted = 0
    </select>

    <insert id="insertMatch">
        INSERT INTO coach_student_match (id, student_id, coach_id, status, create_time, update_time, deleted)
        VALUES (#{id}, #{studentId}, #{coachId}, #{status}, #{createTime}, #{updateTime}, #{deleted})
    </insert>

    <update id="updateMatchStatus">
        UPDATE coach_student_match
        SET status = #{status}, update_time = NOW()
        WHERE student_id = #{studentId}
          AND coach_id = #{coachId}
          AND deleted = 0
    </update>

    <select id="selectByStudentId" resultMap="CoachStudentMatchMap">
        SELECT * FROM coach_student_match
        WHERE student_id = #{studentId}
          AND deleted = 0
          AND status = 'accepted'
    </select>

    <select id="selectByCoachId" resultMap="CoachStudentMatchMap">
        SELECT * FROM coach_student_match
        WHERE coach_id = #{coachId}
          AND deleted = 0
          AND status = 'accepted'
    </select>

    <insert id="insertMatchRequest">
        INSERT INTO coach_student_match (id, student_id, coach_id, status, create_time, update_time, deleted)
        VALUES (#{id}, #{studentId}, #{coachId}, #{status}, #{createTime}, #{updateTime}, #{deleted})
    </insert>

    <select id="selectById" resultMap="CoachStudentMatchMap">
        SELECT * FROM coach_student_match WHERE id = #{id} AND deleted = 0
    </select>

    <select id="selectMatchRequests" resultMap="MatchRequestDetailVOMap">
        SELECT
        csm.id,
        csm.student_id,
        s.name as student_name,
        csm.coach_id,
        c.name as coach_name,
        csm.status,
        csm.create_time,
        csm.update_time
        FROM coach_student_match csm
        JOIN user s ON csm.student_id = s.id
        JOIN user c ON csm.coach_id = c.id
        WHERE csm.deleted = 0
        AND s.deleted = 0
        AND c.deleted = 0
        <choose>
            <when test="userRole == 'student'">
                AND csm.student_id = #{userId}
            </when>
            <when test="userRole == 'coach'">
                AND csm.coach_id = #{userId}
            </when>
        </choose>
        <if test="queryDTO.status != null and queryDTO.status != ''">
            AND csm.status = #{queryDTO.status}
        </if>
        ORDER BY csm.create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countMatchRequests" resultType="long">
        SELECT COUNT(1)
        FROM coach_student_match csm
        JOIN user s ON csm.student_id = s.id
        JOIN user c ON csm.coach_id = c.id
        WHERE csm.deleted = 0
        AND s.deleted = 0
        AND c.deleted = 0
        <choose>
            <when test="userRole == 'student'">
                AND csm.student_id = #{userId}
            </when>
            <when test="userRole == 'coach'">
                AND csm.coach_id = #{userId}
            </when>
        </choose>
        <if test="queryDTO.status != null and queryDTO.status != ''">
            AND csm.status = #{queryDTO.status}
        </if>
    </select>

    <select id="countAcceptedMatchesByStudent" resultType="int">
        SELECT COUNT(1)
        FROM coach_student_match
        WHERE student_id = #{studentId}
          AND status = 'accepted'
          AND deleted = 0
    </select>

    <select id="countAcceptedMatchesByCoach" resultType="int">
        SELECT COUNT(1)
        FROM coach_student_match
        WHERE coach_id = #{coachId}
          AND status = 'accepted'
          AND deleted = 0
    </select>

    <select id="selectAcceptedMatchesByStudent" resultMap="CoachStudentMatchMap">
        SELECT * FROM coach_student_match
        WHERE student_id = #{studentId}
          AND status = 'accepted'
          AND deleted = 0
    </select>

    <select id="selectAcceptedMatchesByCoach" resultMap="CoachStudentMatchMap">
        SELECT * FROM coach_student_match
        WHERE coach_id = #{coachId}
          AND status = 'accepted'
          AND deleted = 0
    </select>

</mapper>